# 🌐 Генератор файла hosts для обхода блокировок

Скрипт для автоматической генерации файла `hosts` из списка доменов. Определяет IP-адреса доменов и создает готовый файл для добавления в системный файл hosts.

## 📋 Возможности

- ✅ Автоматический резолв IP-адресов для списка доменов
- ✅ **Многопоточная обработка** - до 100 параллельных потоков для быстрого резолва
- ✅ **Автоматический поиск похожих доменов** - для неудачных резолвов ищет похожие домены и использует их IP
- ✅ **Резолв вариантов доменов** - пробует разные TLD и поддомены для неудачных доменов
- ✅ **Прогресс-бар** - отслеживание прогресса обработки (с tqdm)
- ✅ Множественные методы резолва (socket, альтернативные DNS серверы)
- ✅ Автоматическая дедупликация доменов
- ✅ Поддержка нескольких файлов с доменами (.txt)
- ✅ Интерактивный выбор файла с доменами
- ✅ Генерация готового файла hosts
- ✅ Автоматическое добавление в системный файл hosts (Linux/macOS)
- ✅ Инструкции по применению для Linux и Windows
- ✅ Обработка ошибок резолва доменов
- ✅ Оптимизировано для больших списков (100K+ доменов)

## 🚀 Установка

### Требования

- Python 3.6 или выше
- Доступ в интернет для резолва DNS

### Опциональные зависимости

Для улучшения работы скрипта рекомендуется установить:

```bash
# Для лучшего резолва заблокированных доменов
pip install dnspython

# Для красивого прогресс-бара
pip install tqdm
```

**dnspython** - позволяет использовать альтернативные DNS серверы (Google DNS, Cloudflare DNS) для обхода блокировок на уровне DNS.

**tqdm** - добавляет красивый прогресс-бар с информацией о скорости обработки и времени до завершения.

### Запуск

1. Клонируйте или скачайте проект
2. Убедитесь, что Python установлен:
   ```bash
   python3 --version
   ```

3. Сделайте скрипт исполняемым (опционально):
   ```bash
   chmod +x hosts_generator.py
   ```

## 📖 Использование

### Подготовка файла с доменами

Создайте файл `general.txt` (или любой другой `.txt` файл) со списком доменов. Каждый домен на новой строке:

```
example.com
google.com
github.com
# Комментарии начинаются с #
```

Поддерживаются домены с префиксами `http://`, `https://`, `www.` - они будут автоматически удалены.

### Запуск скрипта

```bash
python3 hosts_generator.py
```

Скрипт:
1. Найдет все `.txt` файлы в текущей директории
2. Предложит выбрать файл (по умолчанию `general.txt`)
3. Прочитает домены из выбранного файла (автоматически удалит дубликаты)
4. Определит IP-адреса для каждого домена **параллельно** (многопоточность)
5. Покажет прогресс обработки (если установлен tqdm)
6. Создаст файл `hosts` с результатами

**Производительность:**
- Для небольших списков (< 100 доменов): ~10 потоков
- Для средних списков (100-1000 доменов): ~30 потоков  
- Для больших списков (1K-10K доменов): ~50 потоков
- Для очень больших списков (10K+ доменов): до 100 потоков (настраивается)

При обработке 130K доменов скрипт работает в **10-50 раз быстрее** благодаря многопоточности!

**Улучшение покрытия:**
- Автоматически находит похожие домены для неудачных резолвов (например, `a-book.info` → использует IP от `a-book.top`)
- Пробует варианты доменов с разными TLD (`.com`, `.net`, `.org`, `.ru` и др.)
- Может увеличить процент успешных резолвов на **10-30%** для списков с похожими доменами

### Применение файла hosts

#### Linux / macOS

1. Откройте терминал с правами администратора
2. Добавьте содержимое сгенерированного файла в системный hosts:
   ```bash
   sudo cat hosts >> /etc/hosts
   ```
3. Очистите DNS кэш:
   ```bash
   sudo systemd-resolve --flush-caches
   # или
   sudo service network-manager restart
   ```
   
   Для macOS:
   ```bash
   sudo dscacheutil -flushcache
   ```

#### Windows

1. Откройте PowerShell **от имени администратора**
2. Добавьте содержимое файла:
   ```powershell
   Get-Content hosts | Add-Content C:\Windows\System32\drivers\etc\hosts
   ```
3. Очистите DNS кэш:
   ```cmd
   ipconfig /flushdns
   ```

## 📁 Структура проекта

```
.
├── hosts_generator.py  # Основной скрипт
├── general.txt         # Файл с доменами (пример)
├── hosts               # Сгенерированный файл hosts
└── README.md          # Документация
```

## ⚙️ Настройка

### Использование другого файла с доменами

Просто создайте новый `.txt` файл в той же директории. Скрипт автоматически предложит его выбрать при запуске.

### Формат файла с доменами

- Один домен на строку
- Пустые строки игнорируются
- Строки, начинающиеся с `#`, считаются комментариями
- Поддерживаются префиксы `http://`, `https://`, `www.`

Пример:
```
# Основные домены
example.com
https://google.com
www.github.com

# Дополнительные
stackoverflow.com
```

## ⚠️ Важные замечания

1. **Права доступа**: Для редактирования системного файла hosts требуются права администратора
2. **Резервная копия**: Рекомендуется создать резервную копию оригинального файла hosts перед изменением:
   ```bash
   # Linux/macOS
   sudo cp /etc/hosts /etc/hosts.backup
   
   # Windows (PowerShell от администратора)
   Copy-Item C:\Windows\System32\drivers\etc\hosts C:\Windows\System32\drivers\etc\hosts.backup
   ```
3. **DNS кэш**: После изменения файла hosts необходимо очистить DNS кэш
4. **IP-адреса**: IP-адреса могут изменяться, периодически обновляйте файл hosts

## 🐛 Решение проблем

### Домены не резолвятся

Скрипт использует несколько методов резолва для максимальной совместимости:

1. **Стандартный socket** с таймаутом
2. **getaddrinfo** для более надежного резолва
3. **Альтернативные DNS серверы** (Google DNS 8.8.8.8, Cloudflare 1.1.1.1) - требует `dnspython`
4. **Системные утилиты** (nslookup, dig) если доступны

Если домены все еще не резолвятся:

- Установите `dnspython` для использования альтернативных DNS серверов:
  ```bash
  pip install dnspython
  ```
- Проверьте подключение к интернету
- Убедитесь, что домены написаны правильно
- Некоторые домены могут быть полностью заблокированы или не существовать
- Попробуйте запустить скрипт с VPN или другим DNS сервером

### Ошибка доступа к /etc/hosts

- Убедитесь, что используете `sudo` (Linux/macOS)
- Запустите PowerShell от имени администратора (Windows)

### DNS кэш не очищается

- Перезапустите сетевой сервис
- Перезагрузите компьютер

## 📝 Лицензия

Лицензия MIT. Используйте на свой страх и риск.

## 🤝 Вклад

Приветствуются улучшения и предложения!
