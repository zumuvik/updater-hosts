[![Python](https://img.shields.io/badge/python-3.8+-blue.svg)](https://www.python.org/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![GitHub stars](https://img.shields.io/github/stars/zumuvik/updater-hosts?style=social)](https://github.com/zumuvik/updater-hosts)

## В ДАННЫЙ МОМЕНТ ДАННЫЙ КОД РАБОТАЕТ НЕ ТАК КАК НАДО

# 🌐 Генератор файла hosts — обход региональных блокировок

Лёгкий скрипт для генерации готового файла `hosts` из списка доменов. Автоматически резолвит IP-адреса, удаляет дубликаты и может добавить записи в системный `hosts`.

Если нужна англоязычная версия документации — откройте `README.en.md`.

## ✨ Быстрый старт

1. Установите зависимости (рекомендуется в виртуальном окружении):

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

2. Подготовьте файл `general.txt` (по одному домену на строку).

3. Запустите:

```bash
python3 hosts_generator.py
```

4. По умолчанию создаётся локальный файл `hosts`. Скрипт также автоматически сохранит резервную копию системного `hosts` в `hosts.backup` при старте.

## 🚀 Основные возможности

- Параллельный резолв доменов (по умолчанию — разумное число потоков)
- Поддержка альтернативных DNS через `dnspython` (Google/Cloudflare)
- Прогресс-бар при наличии `tqdm`
- Поиск похожих доменов и попытки вариантов TLD для повышения успешности
- Автоматическое создание резервной копии системного `hosts` в `hosts.backup`
- Интерактивный выбор входного `.txt` файла

## 🛠 Установка зависимостей

Зависимости указаны в `requirements.txt`. Установите их так:

```bash
pip install -r requirements.txt
```

Опционально можно установить только нужные пакеты:

```bash
pip install dnspython tqdm
```

## ⚠️ Важные замечания и безопасность

- Для изменения системного файла `hosts` требуются права администратора.
- Скрипт при старте пытается сохранить текущий системный `hosts` в `hosts.backup` в рабочей директории. Если это не удаётся — вы получите предупреждение.
- После изменения `hosts` может потребоваться очистить DNS-кеш (команды зависят от ОС).
- Рекомендуется запускать данный скрипт раз в день или поставить на автозапуск
## Примеры использования

1) Создать `general.txt`, запустить и посмотреть `hosts`:

```bash
echo example.com > general.txt
python3 hosts_generator.py
cat hosts
```

2) Добавить сгенерированные записи в системный `hosts` (Linux/macOS):

```bash
sudo cat hosts >> /etc/hosts
```

## Справка и отладка

Если домены не резолвятся — установите `dnspython`, проверьте сеть и попробуйте другой DNS/VPN.

## 📁 Структура проекта

```
. 
├── hosts_generator.py   # Скрипт генерации
├── general.txt          # Пример: список доменов
├── hosts                # Сгенерированный выход
├── hosts.backup         # (создаётся автоматически при старте)
└── README.en.md         # Англоязычная версия документации
```

## Лицензия

MIT — см. файл `LICENSE`.

## Вклад 

PR и issue приветствуются. Начните с fork → branch → PR.

## 🤖 О разработке проекта

Этот проект был разработан с использованием AI-ассистента (LLM Agent). Исходный код был сгенерирован, тщательно в ручную проверен, оптимизирован и адаптирован для использования в production среде.
